package Game;

/**
 * The interface for the move of the Dots and Boxes game.
 */
public interface Move {
    /**
     * Returns the index of the move.
     * @return the index of the move.
     */
    int getIndex();
}
